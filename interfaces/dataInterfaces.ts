export interface SelectionType {
        id: number;
        title: string;  
}

export interface titleTable {
        id: number;
        title: string;
}

export interface formData {
        propriedade: string
}